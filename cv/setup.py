import setuptools

setuptools.setup(
    name='draw',
    packages=['draw'],
    # This sets the directory, src, to be the root package (denoted by '').                    
    package_dir={'': 'src'}
)
