import setuptools

setuptools.setup(
    name='project1',
    packages=['project1'],
    # This sets the directory, src, to be the root package (denoted by '').                    
    package_dir={'', 'src'}
)
